/** @type {import('next').NextConfig} */
const nextConfig = {};

export default {
    // Your Next.js configuration options here
    images: {
        domains: ['utfs.io'],
    },
};
// export default nextConfig;
